# 引き継ぎドキュメント — 2026-05-22

> 次回作業開始前にこのファイルと `KNOWLEDGE.md` を読むこと。
> 設計ドキュメントは `docs/DESIGN_DOMESTIC.md` を参照。

---

## 本日やったこと

### 1. 内政パート 設計完了 → `docs/DESIGN_DOMESTIC.md` に記録済み

以下を設計・確定した。

| 機能 | 状態 |
|------|------|
| 行動力（AP）システム | 設計確定 |
| 研究ツリー（ターン制・直列キュー） | 設計確定 |
| キャラクター強化コマンド | 設計確定（コマンド全リストは未定） |
| 劇場（Theater）— 訪問イベント統合 | 設計確定 |
| 秘書ウィジェット（PartnerWidget） | 設計確定 |

**主要決定事項:**
- 行動力: 5/ターン、全アクション共有、0でも手動ターンエンド
- 研究: ミーム投入→Nターン後完了、直列1本のみ、パラメータ直接変更なし
- 劇場カテゴリ: visit / main / character / recurring（この順で表示）
- 防衛プロンプト: ADVシーンではなくマップ上オーバーレイ（実装コスト優先）
- 秘書設定: PartySceneに「秘書に設定」ボタン追加、セーブデータに保存

---

### 2. エディタ機能調査（事前調査のみ）

EventEngine.js と実データJSONを読んで、実装側の全機能を洗い出した。
**エディタ未対応の可能性が高い項目:**

- トリガー: `before_faction_turn`
- 条件: `turnAfterFlag` / `defeatedChar` / `noOther`
- エフェクト: `dungeonUnlock` / `itemGain` / `itemLose` / `charUsedThisTurn` / `legionForceAttack` / `legionUpdate`
- スクリプト: `choice` の `effectsKey` 分岐
- トリガー `base_visit` / `base_defense` はGameContextからも未発火

**→ 次回: エディタ（`node tools/editor.js` @ localhost:3001）を開いて上記項目の対応状況を確認し、不足分を洗い出してタスク化する**

---

## 次にやること（優先度順）

### 高優先
1. **内政パート実装**（`docs/DESIGN_DOMESTIC.md` の §7 実装順序に従う）
   - Step 1: GameContext に `actionPoints` / `maxActionPoints` / `researchQueue` / `upgradeUnlocks` / `secretaryId` を追加
   - Step 2: TopBar に行動力表示
   - Step 3: 研究をターン制に変更
   - Step 4: PartyScene にキャラ強化・秘書設定を追加
   - Step 5: PartnerWidget 新規作成
   - Step 6: MapScene に PartnerWidget 組み込み・防衛プロンプト対応
   - Step 7: TheaterScene 新規作成
   - Step 8: BottomBar に劇場ボタン追加

2. **エディタ機能追加**（帰宅後に確認してからタスク化）

### 中優先
3. `adv` / `dungeon` — GameContext・EventEngine 接続（`KNOWLEDGE.md §12` 残タスク1）
4. `new_game_plus` — 実データ接続

### 低優先
5. `gallery` / `settings` / `credits` 実装

---

## 未決事項（ゲームデザイン側で決める必要あり）

- キャラ強化コマンドの追加リスト（研究アンロック分）
- 研究ツリーの全ノード定義
- 繰り返し劇場イベントのユースケース
- `secretary_lines.json` の各キャラ台詞テキスト

---

## 参照先まとめ

| ドキュメント | 内容 |
|-------------|------|
| `KNOWLEDGE.md` | プロジェクト全体のテックリード引き継ぎ（最新仕様） |
| `docs/DESIGN_DOMESTIC.md` | 内政パート・秘書ウィジェット設計 |
| `docs/archive/TASK_*.md` | 過去の実装タスク（完了済み参考資料） |
